import { Inter, Lato, Lusitana, Raleway } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const raleway = Raleway({
    weight: ['300', '500'],
    subsets: ['latin']
  });

  // export const lato = Lato({
  //   weight: ['400'],
  //   subsets: ['latin']
  // });